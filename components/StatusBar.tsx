'use client';import {CircleDot,ShieldCheck,Zap} from 'lucide-react';export function StatusBar({valid,steps}:{valid:boolean;steps:number}){return <div className="h-8 border-t border-[var(--border)] bg-[var(--panel)] px-3 flex items-center gap-4 text-[10px] text-[var(--muted)]"><span className="flex items-center gap-1"><CircleDot size={10}/>{steps} nodes</span><span className="flex items-center gap-1">{valid?<ShieldCheck size={10}/>:<Zap size={10}/>} {valid?'Workflow valid':'Needs attention'}</span><span className="ml-auto mono">LOCAL-FIRST · VERCEL HOBBY</span></div>}
