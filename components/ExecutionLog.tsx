'use client';import type {ExecutionLog} from '@/types/workflow';export function ExecutionLogPanel({logs}:{logs:ExecutionLog[]}){return <div className="space-y-2">{logs.map((l,i)=><div key={i} className="flex gap-2 text-[10px]"><span className={l.level==='error'?'text-red-500':l.level==='success'?'text-emerald-500':'text-[var(--muted)]'}>●</span><span className="flex-1">{l.message}</span><span className="mono text-[var(--muted)]">{l.durationMs??0}ms</span></div>)}</div>}
